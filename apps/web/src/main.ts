import './style.css';
const api='http://127.0.0.1:8000';
document.querySelector<HTMLDivElement>('#app')!.innerHTML=`<header><h1>Simulateur LiDAR France</h1><span>CPU · IGN/local · sans OSM</span></header><main><section><h2>Projet de démonstration</h2><label>Profil <select id="profile"><option>surface</option><option>standard</option><option>quality</option></select></label><label>Fidélité <input id="fidelity" type="range" min="0" max="100" value="45"><output id="fv">45</output></label><pre id="estimate"></pre><button id="build">Construire la map</button><pre id="result"></pre></section><section><h2>LiDAR</h2><button id="classes">Inventorier les classes</button><pre id="classout"></pre><p>Classes démo : sol 2, végétation 5, bâtiment 6. La visionneuse complète iTowns est préparée comme extension; le MVP protège la mémoire en exposant d'abord les statistiques et chunks.</p></section></main>`;
const f=document.querySelector<HTMLInputElement>('#fidelity')!, profile=document.querySelector<HTMLSelectElement>('#profile')!;
async function estimate(){document.querySelector('#fv')!.textContent=f.value; const r=await fetch(`${api}/profiles/${profile.value}/estimate?fidelity=${f.value}`); document.querySelector('#estimate')!.textContent=JSON.stringify(await r.json(),null,2)}
f.oninput=estimate; profile.onchange=estimate; estimate();
document.querySelector('#build')!.addEventListener('click',async()=>{const r=await fetch(`${api}/demo/build`,{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({profile:profile.value,fidelity:+f.value})});document.querySelector('#result')!.textContent=JSON.stringify(await r.json(),null,2)});
document.querySelector('#classes')!.addEventListener('click',async()=>{const r=await fetch(`${api}/projects/demo/lidar/classes`);document.querySelector('#classout')!.textContent=JSON.stringify(await r.json(),null,2)});
